# Project Memory

- **Phase 1 App decomposition module split (2026-02-27)**: **What** session event/feed/toast normalization helpers were moved out of `App.jsx` into a dedicated library module; session action shaping/error helpers were moved to their own library module. **Where** `src/lib/session-event-normalize.js`, `src/lib/session-event-normalize.test.js`, `src/lib/session-action-helpers.js`, `src/lib/session-action-helpers.test.js`. **Evidence** `App.jsx` now imports these helpers and both new test files pass in `vitest`.
- **Phase 1 multiplayer UI side-effect hooks (2026-02-27)**: **What** session action submit flow, remote roll toast ingestion, and GM action orchestration were extracted from `App.jsx` into focused hooks. **Where** `src/hooks/useSessionActionSubmit.js`, `src/hooks/useRemoteRollToasts.js`, `src/hooks/useGmActions.js`, consumed by `src/App.jsx`. **Evidence** existing integration tests in `src/App.join-flow.test.jsx` and `src/App.toast-integration.test.jsx` remain green.
- **Phase 1 view decomposition for multiplayer shells (2026-02-27)**: **What** auth-lost/session wrappers and GM controls panel were extracted into dedicated components, reducing top-level app coupling. **Where** `src/components/MultiplayerAuthLostView.jsx`, `src/components/SessionView.jsx`, `src/components/GmControlsPanel.jsx`. **Evidence** `App.jsx` now routes through imported components and still exports `REMOTE_ROLL_EVENT_BRIDGE_KEY` for toast tests.
- **Phase 2 multiplayer session decomposition (2026-02-27)**: **What** polling and normalization helpers were split out of `useMultiplayerSession`, including a dedicated polling hook and a reusable multiplayer normalization utility module. **Where** `src/hooks/useEventPolling.js` + `src/lib/multiplayer-normalize.js` (with tests in `src/lib/multiplayer-normalize.test.js`), consumed by `src/hooks/useMultiplayerSession.js`. **Evidence** `useMultiplayerSession` now imports these modules and retains passing polling/GM integration tests in `src/hooks/useMultiplayerSession.test.jsx`.

- **Character import utilities** live in `src/lib/character-import.js` (parses JSON, validates required attribute dice, defaults missing skills to 0, builds display name).
- **API readiness closure checklist**: **What** implementation readiness is now documented as a contract-first blocker list (event cursor semantics, actor identity authority, GM player management, snapshot/error schemas, transaction guarantees, and test matrix) with ordered execution phases and sign-off criteria. **Where** `docs/plans/implementation-readiness-summary.md`. **Evidence** doc now contains 12 explicit blocking tasks plus implementation order.
- **Canonical multiplayer API spec**: **What** API contract and execution order are now consolidated into one normative v1 specification for both server and client implementation, including endpoint schemas, error envelope, event semantics, transaction guarantees, and phased test matrix. **Where** `docs/plans/multiplayer-api-contract-and-build-spec.md`, with aligned runbooks in `docs/plans/server-side-implementation-outline.md` and `docs/plans/client-side-implementation-spec.md`. **Evidence** readiness summary now marks all 12 blockers closed and references these documents.
- **Session state key/value authority**: **What** session-scoped mutable values now use `session_state.state_name/state_value` as the single source of truth for both `scene_strain` and `joining_enabled`; `joining_enabled` is stored as `"true"`/`"false"` and parsed to boolean on read. **Where** `docs/plans/multiplayer-api-contract-and-build-spec.md` (Section 6 constraints and typed conversion rules) and `docs/plans/server-side-implementation-outline.md` (data model + validation/transaction requirements). **Evidence** create/join/toggle route behavior now references `session_state` for `joining_enabled`.
- **Imported skill dice bounds**: **What** skill dice values are only accepted in the inclusive range `0..10`; any imported skill below `0`, above `10`, or non-integer is normalized to `0` with a warning. **Where** `src/lib/character-import.js` (`toSkillDiceCount`, `parseCharacterImport`). **Evidence** tests `parseCharacterImport sets skill dice below zero to 0` and `parseCharacterImport sets skill dice above 10 to 0` in `src/lib/character-import.test.js`.
- **Canonical skill mappings** live in `src/data/skill-mappings.js` (skill → attribute map used by import flow).
- **Import UI tabs** are implemented in `src/components/DicePoolPanel.jsx` (manual vs import with file input, selectors, and roll/push handling).
- **Import state hook** lives in `src/hooks/useCharacterImport.js` (file loading, validation state, selection state).
- **App integration** wires Dice Pool tabs and roll overrides in `src/App.jsx`.
- **Import summary quick-rolls**: **What** clicking attribute/skill counts triggers rolls with the selected dice. **Where** `src/components/DicePoolPanel.jsx` (`handleSummaryRoll`). **Evidence** summary list items are rendered as buttons that call `handleSummaryRoll`.
- **Full-page dice stage**: **What** the 3D dice canvas is now rendered in a fixed, fullscreen layer behind the UI. **Where** `src/App.jsx` (`dice-stage-fullscreen` wrapper) and `src/App.css` (fullscreen positioning + canvas sizing). **Evidence** the dice tray section was removed and the new wrapper sits above `main` content.
- **Theme styling centralization**: **What** app chrome colors are primarily controlled through CSS variables and component class rules in a single stylesheet. **Where** `src/App.css` (`:root` design tokens + panel/tab/tray selectors). **Evidence** most visual surfaces read from `--ink-*`, `--surface-*`, `--panel`, and related tokens.
- **Global color-scheme integration**: **What** browser form/control theming now tracks resolved app theme (`light`/`dark`) via `data-theme` on document root. **Where** `src/index.css` (`:root[data-theme="dark"]`, `:root[data-theme="light"]`) and `src/App.jsx` (root attribute update effect). **Evidence** `system` mode automatically flips when OS preference changes.
- **Theme preference state flow**: **What** theme preference (`light`, `dark`, `system`) is persisted and resolved from OS setting when `system` is selected. **Where** `src/lib/theme-preference.js` + `src/hooks/useThemePreference.js`. **Evidence** hook loads/saves preference and subscribes to `prefers-color-scheme` media query changes.
- **Theme application boundary**: **What** resolved theme is applied at the document root and consumed by CSS token overrides. **Where** `src/App.jsx` (`document.documentElement.setAttribute("data-theme", resolvedTheme)`) and `src/App.css` (`:root[data-theme="dark"]` token set). **Evidence** header now includes a `Theme` selector and styles switch via token-driven values.
- **Toast architecture reference from Fate Cards**: **What** the reusable toast mechanism follows a context provider + container + hook split with timer-managed auto-dismiss and safe fallback handlers. **Where** reference files in `/tmp/fate-cards/src/components/toast/*.jsx`, `/tmp/fate-cards/src/components/toast/Toast.css`, and `/tmp/fate-cards/src/hooks/useToast.js`; Year Zero integration targets `src/main.jsx` + `src/App.jsx`. **Evidence** Fate Cards wires `<ToastProvider>` in `main.jsx` and emits dice-result toasts from `App.jsx` via `toast.diceResult(...)`.
- **Source-agnostic roll toast design**: **What** roll result toasts are planned to be emitted from a normalized roll-event adapter that accepts both local and remote/API data. **Where** plan in `docs/plans/2026-02-18 Toast Alert Integration Plan.md` and proposed helper `src/lib/roll-toast-event.js`. **Evidence** plan defines normalized event fields (`eventId`, `source`, `actorName`, `action`, outcomes, timestamp) plus dedupe key strategy.
- **Roll toast defaults and overflow policy**: **What** roll-result notifications are fixed to `diceResult` mode, `10000ms` duration, actor label from `actorId`, and pending queue overflow strategy of drop-oldest with max 20 pending. **Where** `docs/plans/2026-02-18 Toast Alert Integration Plan.md` and `docs/DECISIONS.md` (2026-02-18 entry). **Evidence** user-confirmed answers applied to plan and decisions log.
- **Primary action controls location**: **What** push/clear controls now live in the main dice pool panel footer instead of the removed tray results panel, with desktop side-by-side and mobile stacked layout. **Where** `src/components/DicePoolPanel.jsx` (`panel-action-row`) and `src/App.css` (`.panel-action-row`, responsive overrides). **Evidence** App now passes push/clear props directly to `DicePoolPanel` and no longer renders `.tray-panel`.
- **Manual tab input lifecycle**: **What** Manual attribute/skill number fields now keep draft string values while editing (including empty strings), and are normalized only when `Roll Dice` is pressed. **Where** `src/components/DicePoolPanel.jsx` (`manualAttributeInput`, `manualSkillInput`, `commitManualCounts`, `handleManualPrimaryAction`). **Evidence** tests `manual inputs can be cleared while editing` and `manual roll validates and commits counts before rolling` in `src/components/DicePoolPanel.test.jsx`.
- **Toast stack shift animation**: **What** regular toast items now animate upward when their layout position changes (for example when a new toast appears), using a FLIP-style `getBoundingClientRect` delta in `useLayoutEffect`; dice result text is set to `1.5rem`. **Where** `src/components/toast/ToastContainer.jsx` (`toastPositionMapRef` and layout effect) and `src/components/toast/Toast.css` (`.toast-result-text`). **Evidence** toast list rerender test in `src/components/toast/ToastContainer.test.jsx` covers add-toast update path.
- **Clear action toast guard**: **What** local toast emission now requires a non-null `currentRoll`, preventing stale-history clear actions from being normalized into synthetic `0 successes, 0 banes` toasts after dedupe TTL expiry. **Where** `src/App.jsx` (local roll toast `useEffect`). **Evidence** `clear state does not emit a synthetic local roll toast` in `src/App.toast-integration.test.jsx`.
- **Toast timeout progress indicator**: **What** timed toasts render a bottom fill bar that progresses from 0% to 100% over each toast's `duration` so expiry timing is visible. **Where** `src/components/toast/ToastContainer.jsx` (`toast-progress-bar` render gate on positive `duration`) and `src/components/toast/Toast.css` (`@keyframes toast-progress-fill`). **Evidence** `renders a timed progress bar when toast duration is set` in `src/components/toast/ToastContainer.test.jsx`.
- **Client API base URL config utility**: **What** client-side API endpoint configuration is centralized in a dedicated helper that reads `VITE_API_BASE_URL`, validates non-empty string values when provided, normalizes trailing slashes, and falls back to same-origin `"/api"` when unset. **Where** `src/lib/app-config.js` (`getApiBaseUrl`, `normalizeApiBaseUrl`, `buildApiUrl`). **Evidence** unit coverage in `src/lib/app-config.test.js` for fallback, validation, normalization, and URL composition paths.
- **Shared API client foundation (C1)**: **What** a contract-aware fetch layer now exists with bearer token attachment, JSON success parsing, `204` no-content handling, and normalized non-2xx exceptions exposing `status`, `code`, `message`, and optional `details`. **Where** `src/lib/api-client.js` (`apiFetch`, `apiGet`, `apiPost`, `ApiClientError`, `parseApiErrorEnvelope`). **Evidence** tests in `src/lib/api-client.test.js` cover success parsing, envelope parsing, fallback HTTP status codes, and malformed JSON handling.
- **Join route and token handoff foundation (C2)**: **What** the app now handles `/join` routes by parsing `#join=<token>`, rendering a dedicated join view, posting `display_name` to `/api/join`, storing returned session auth in memory-only state, clearing hash fragments, and navigating out of the join path on success. **Where** `src/App.jsx` (route split + handoff), `src/components/JoinSessionView.jsx`, `src/lib/join-session-route.js`, `src/lib/session-auth.js`. **Evidence** tests in `src/components/JoinSessionView.test.jsx`, `src/App.join-flow.test.jsx`, `src/lib/join-session-route.test.js`, and `src/lib/session-auth.test.js`.
- **Session snapshot bootstrap hook (C3)**: **What** multiplayer bootstrap now requests `/api/session` from memory-held auth, normalizes snapshot state (`sessionId`, `role`, `self`, `sceneStrain`, `players`, `sinceId`), and handles auth-loss by clearing in-memory auth with explicit `auth_lost` state. **Where** `src/hooks/useMultiplayerSession.js` and `src/lib/session-snapshot.js`. **Evidence** tests in `src/hooks/useMultiplayerSession.test.jsx` and `src/lib/session-snapshot.test.js`.
- **Polling engine contract implementation (C4)**: **What** the multiplayer hook now schedules event polling with contract interval semantics (`1000ms` start, `x1.5` idle backoff to `8000ms`, exponential error backoff with jitter to `30000ms`), appends events, advances `sinceId`, and clears auth/stops polling on `401`/`403` token failures. **Where** `src/hooks/useMultiplayerSession.js`. **Evidence** timer-driven tests in `src/hooks/useMultiplayerSession.test.jsx` for `200`, `204`, non-auth error, and auth-failure shutdown paths.
- **Event reducer + action submit foundation (C5)**: **What** multiplayer session state now applies incoming events through a typed reducer (`roll`, `push`, `strain_reset`, `join`, `leave`) with dedupe by `event.id`, and exposes validated `submitRoll`/`submitPush` actions that post only `{ type, payload }` then treat server responses as authoritative for appended events and scene strain updates. **Where** `src/lib/multiplayer-event-reducer.js` and `src/hooks/useMultiplayerSession.js`. **Evidence** reducer tests in `src/lib/multiplayer-event-reducer.test.js` and submit/reducer tests in `src/hooks/useMultiplayerSession.test.jsx`.
- **GM control API methods (C6)**: **What** the multiplayer hook now exposes GM operations (rotate join link, toggle joining, reset scene strain, refresh players, revoke player) with role/session/token guardrails and auth-failure shutdown behavior, and applies response-driven state updates (`joiningEnabled`, `sceneStrain`, `players`, cursor advancement). **Where** `src/hooks/useMultiplayerSession.js` (`rotateJoinLink`, `setJoiningEnabled`, `resetSceneStrain`, `refreshPlayers`, `revokePlayer`). **Evidence** GM-path tests in `src/hooks/useMultiplayerSession.test.jsx` cover role-forbidden, happy-path updates, and revoke cursor/state behavior.
- **Top-level multiplayer UI mode switch (Task 1)**: **What** `App` now resolves explicit modes (`host`, `join`, `session`, `auth_lost`) from route + in-memory auth + `sessionState.status`; session bootstrap is triggered only in `session` mode while host/auth-lost use dedicated shell views. **Where** `src/App.jsx` (`resolveMultiplayerMode`, `MultiplayerHostView`, `MultiplayerAuthLostView`, mode-gated return flow). **Evidence** tests in `src/App.join-flow.test.jsx` cover host, join, session bootstrap trigger, and auth-lost reset path.
- **Host-session creation flow (Task 2)**: **What** host mode now renders a create-session form that posts `session_name` to `/api/sessions`, maps contract errors, normalizes `gm_token` into in-memory auth (`sessionToken`, `sessionId`, `role: "gm"`), and hands off to existing session bootstrap mode. **Where** `src/components/HostSessionView.jsx` and `src/App.jsx` (`handleHostSuccess`). **Evidence** tests in `src/components/HostSessionView.test.jsx` and `src/App.join-flow.test.jsx` cover request shape, success handoff, and bootstrap trigger.
- **Invite-link intake fallback (Task 3)**: **What** players can now paste full invite links (or raw join tokens) both from host entry mode and the `/join` missing-token fallback; parsed tokens are routed to canonical `/join#join=<token>` intake without bypassing existing join flow. **Where** `src/lib/join-session-route.js` (`parseJoinTokenFromInviteInput`, `buildJoinPathWithToken`), `src/components/HostSessionView.jsx`, `src/components/JoinSessionView.jsx`, and `src/App.jsx` (`handleUseInviteLink`). **Evidence** tests in `src/lib/join-session-route.test.js`, `src/components/HostSessionView.test.jsx`, `src/components/JoinSessionView.test.jsx`, and `src/App.join-flow.test.jsx`.
- **Dedicated session-mode wrapper with multiplayer summary (Task 4)**: **What** session mode now renders through `SessionView`, which triggers `bootstrapFromAuth` when a token exists and session state is `idle`, and surfaces a multiplayer summary strip (connection status, role badge, session name, scene strain, player count) above the existing dice UI. **Where** `src/App.jsx` (`SessionView`, `buildConnectionSummary`, `DiceRollerApp` `sessionSummary` prop) and `src/App.css` (`session-summary-*` styles + responsive layout). **Evidence** `src/App.join-flow.test.jsx` now covers summary rendering in ready state while preserving bootstrap behavior for idle state.
- **Local roll/push submit wiring with pending/error UX (Task 5)**: **What** resolved local roll/push outcomes are now submitted to multiplayer APIs via `submitRoll`/`submitPush`, with id-based dedupe, pending-submit control lockout, and non-fatal action error display in the session summary panel. **Where** `src/App.jsx` (`buildSessionActionRequest`, submit `useEffect`, `session-action-error` rendering), `src/components/DicePoolPanel.jsx` (`isActionSubmitPending` control disabling), and `src/App.css` (`.session-action-error`). **Evidence** new tests in `src/App.join-flow.test.jsx` verify roll/push payload submission, pending disable behavior, and error surfacing.
- **Submit-pending state StrictMode fix**: **What** action-submit pending state now reliably clears in development StrictMode because the mounted guard is reset on effect mount (not only cleanup). **Where** `src/App.jsx` (`isMountedRef` effect in `DiceRollerApp`). **Evidence** pending flag transitions in `src/App.join-flow.test.jsx` continue to pass while avoiding persistent `Submitting...` in real UI.
- **Submit-lock label behavior**: **What** manual primary button text now remains `Roll Dice` during submit lockout; pending state is conveyed via disabled controls instead of swapping button text. **Where** `src/components/DicePoolPanel.jsx`. **Evidence** `keeps primary action label while action submit is pending` test in `src/components/DicePoolPanel.test.jsx`.
- **Connection label semantics for idle polling**: **What** `204 No Content` poll responses now keep polling status as `running` (connected idle) while only error backoff uses `backoff`/`Reconnecting`; transient error recovery clears error fields on next successful idle poll. **Where** `src/hooks/useMultiplayerSession.js` (`runPollCycle` 204 branch) and `src/hooks/useMultiplayerSession.test.jsx`. **Evidence** tests `polling loop increases interval on 204 no-content responses while staying connected` and `polling loop returns to connected state after a transient error`.
- **Session event feed panel (Task 6)**: **What** session mode now renders a lightweight ordered event feed sourced from `sessionState.events`, with per-type summaries for `roll`, `push`, `join`, `leave`, and `strain_reset`, and dedupe-safe rendering keyed by `event.id`. **Where** `src/App.jsx` (`normalizeSessionEventsForFeed`, `buildSessionEventSummary`, `session-events-feed` section) and `src/App.css` (`session-events-*` styles). **Evidence** `session mode renders ordered multiplayer event feed entries` in `src/App.join-flow.test.jsx`.
- **GM controls panel (Task 7)**: **What** session mode now renders a GM-only host tools panel (rotate join link, toggle joining, reset scene strain, refresh/revoke players) with action-level pending/error/message feedback and a role-gated player roster UI. **Where** `src/App.jsx` (`resolvedGmControls`, `gm-controls-panel`, `runGmAction`) and `src/App.css` (`gm-controls-*`, `gm-player-*` styles). **Evidence** tests `session mode only renders GM controls for gm role` and `session mode wires gm control actions and revoke player action` in `src/App.join-flow.test.jsx`.
- **Polling indicator UI coverage (Task 9)**: **What** top-level join-flow tests now explicitly verify session summary connection labels for both polling backoff (`Reconnecting`) and session bootstrap/transport failure state (`Connection Error`). **Where** `src/App.join-flow.test.jsx`. **Evidence** tests `session mode shows reconnecting label while polling is in backoff` and `session mode shows connection error label when session state is error`.
- **GM join-link copy action (Task 10)**: **What** GM controls now include a dedicated `Copy Join Link` action after link rotation, using Clipboard API first with an `execCommand("copy")` fallback and explicit success/error feedback. **Where** `src/App.jsx` (`copyTextToClipboard`, `handleCopyJoinLink`, `gm-copy-link-button`) and `src/App.css` (`gm-controls-link-row`). **Evidence** `session mode wires gm control actions and revoke player action` now asserts clipboard write and `Join link copied.` message in `src/App.join-flow.test.jsx`.
- **Session error retry action (Task 10)**: **What** session summary now exposes bootstrap error details with a `Retry Connection` action that re-invokes `bootstrapFromAuth` when session status is `error`, with a ref-backed in-flight guard that prevents duplicate retry clicks until the first retry resolves and a live status announcement (`Retrying connection...`) while retry is in progress. **Where** `src/App.jsx` (`sessionConnectionMeta`, `session-retry-button`, `session-connection-error`, `session-retry-status`, `retryPendingRef`) and `src/App.css` (`session-connection-error-row`, `session-retry-status`). **Evidence** tests `session mode shows connection error label when session state is error` and `session mode retry button re-runs bootstrap once while request is pending` in `src/App.join-flow.test.jsx`.
- **GM action status announcements (Task 10)**: **What** GM controls now expose live status announcements for pending and successful actions via `role="status"`/`aria-live="polite"` messaging, while errors remain `role="alert"`. **Where** `src/App.jsx` (`gm-action-pending`, `gm-action-message`). **Evidence** `session mode wires gm control actions and revoke player action` now asserts pending status during deferred rotate action in `src/App.join-flow.test.jsx`.
- **Secondary-action keyboard focus visibility (Task 10)**: **What** all secondary action buttons (join, retry, GM controls, copy link) now use the shared custom `:focus-visible` ring so keyboard navigation has consistent focus affordances. **Where** `src/App.css` (`.join-secondary:focus-visible` in the focus rule group). **Evidence** class is used by join/GM/session retry actions rendered in `src/App.jsx`, `src/components/JoinSessionView.jsx`, and `src/components/HostSessionView.jsx`.
- **Cross-domain API CORS rollout checklist**: **What** deployment guidance for hosting API on a different origin now exists as an explicit environment checklist (origin allowlist, preflight handling, error-path CORS headers, bearer-token constraints, and verification steps). **Where** `docs/plans/cors-deployment-checklist.md`. **Evidence** checklist includes required headers and production/staging/dev origin scoping.
- **Environment-driven CSP `connect-src` for API domains**: **What** CSP now allows API connections to configured external origins via build-time env while keeping `'self'` as baseline; deployments set `VITE_CSP_CONNECT_SRC` alongside `VITE_API_BASE_URL`. **Where** `index.html` (`Content-Security-Policy` meta), `.env.development`, `.env.staging`, `.env.production`, and `docs/plans/cors-deployment-checklist.md`. **Evidence** CSP now includes `connect-src 'self' %VITE_CSP_CONNECT_SRC%`.
- **Unified multiplayer strain authority**: **What** session mode now uses authoritative session strain (`sessionState.sceneStrain`) for both top-bar `Strain Points` display and roll strain-dice construction, and stops local bane-based strain accumulation while in session mode. Top-bar reset routes to local reset only outside session mode; in session mode it is role-gated to GM reset API action. **Where** `src/App.jsx` (`DiceRollerApp` `resolvedStrainPoints`, authoritative `useRollSession` wiring, top-bar reset handler), `src/App.join-flow.test.jsx` (authoritative strain + reset routing assertions). **Evidence** tests verify top pill value and roll-session normalized strain follow multiplayer state; non-GM reset is disabled and GM reset invokes `resetSceneStrain`.
- **Polling timeout watchdog and 5s caps**: **What** multiplayer event polling now enforces a per-request timeout watchdog (`5000ms`) and caps both idle and error backoff intervals at `5000ms`; stalled event requests now transition to backoff and schedule the next cycle instead of hanging indefinitely. **Where** `src/hooks/useMultiplayerSession.js` (`POLL_REQUEST_TIMEOUT_MS`, `requestEventsWithTimeout`, `MAX_IDLE_POLL_INTERVAL_MS`, `MAX_ERROR_POLL_INTERVAL_MS`) plus signal passthrough in `src/lib/api-client.js`. **Evidence** `polling loop times out stalled event requests and keeps polling` in `src/hooks/useMultiplayerSession.test.jsx`.
- **Session roll/push remote toast integration**: **What** session-mode event stream entries of type `roll`/`push` now emit dice-result toasts through the existing toast pipeline using actor-aware titles (`Roll Result - <name>` / `Push Result - <name>`), while self-authored session events are suppressed to avoid duplicate local+remote notifications. **Where** `src/App.jsx` (`isSessionRollEventFromSelf`, session-events toast effect in `DiceRollerApp`) and `src/lib/roll-toast-event.js` (`buildRollToastPayload`). **Evidence** tests `session mode emits remote roll and push toasts with actor names` and `session mode skips remote roll toasts for self-authored events` in `src/App.join-flow.test.jsx`.
